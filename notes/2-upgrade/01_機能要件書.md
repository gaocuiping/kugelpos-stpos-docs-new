# アプリケーションバージョン管理システム - 機能要件書

## 1. 概要

### 1.1 目的

Kugelposシステムにおけるコンテナイメージ、スクリプト、設定ファイル等の配布物を一元管理し、クラウド・エッジ・POS端末間で効率的かつセキュアにバージョン管理・更新を実現する。

### 1.2 背景

- クラウド、エッジ、POS端末で動作する複数のマイクロサービスのバージョンを統一的に管理する必要がある
- ネットワーク障害時でもエッジ・POS端末が動作を継続できるよう、オフライン耐性が必要
- セキュリティパッチや機能追加を迅速に全環境に展開する必要がある
- startup.sh等のスクリプト自身も安全に更新できる仕組みが必要

### 1.3 スコープ

本サービスは以下の配布物のバージョン管理を対象とする：
- **コンテナイメージ**: account, terminal, master-data, cart, report, journal, stock, sync（クラウド→エッジ、クラウド→POS）
- **スクリプトファイル**: edge-startup.sh, pos-startup.sh（クラウド→エッジ、クラウド→POS）
- **設定ファイル**: docker-compose.yml, 環境設定ファイル（クラウド→エッジ、クラウド→POS）
- **モジュールファイル**: Python wheels, システムパッケージ（クラウド→エッジ、クラウド→POS）
- **画像・ドキュメント**: UIアセット、マニュアル（クラウド→エッジ、クラウド→POS）

## 2. 機能要件

### 2.1 サービス構成

#### 2.1.1 新規機能の追加

- **バージョン管理API**: Sync Service内に実装
- **Artifact Management API**: ファイル配布・ダウンロード機能
- **Manifest Generation**: デバイス別更新内容の生成

#### 2.1.2 サービス配置

```
クラウド環境:
  - Azure Container Registry (ACR): コンテナイメージ管理
  - Azure Blob Storage: スクリプト・設定ファイル管理
  - Sync Service: バージョン管理API、Manifest配信、エッジデバイス認証

エッジ環境:
  - Edge Registry (Harbor/Docker Registry): イメージキャッシュ
  - startup.sh: 自動更新スクリプト
  - Services: 各マイクロサービス

POS端末環境:
  - startup.sh: 自動更新スクリプト
  - Services: 各マイクロサービス

注記: POS端末もバージョン管理においてはエッジデバイスとして扱われ、edge_idで認証
```

### 2.2 バージョン管理仕様

#### 2.2.1 バージョン管理対象一覧

| 配布物種別 | 配布方向 | 差分更新 | 一括更新 | チェック間隔 | 備考 |
|------------|----------|----------|----------|----------------|------|
| container_images | クラウド→エッジ/POS | ✓ | ✓ | 起動時・15分 | Dockerレイヤー差分 |
| scripts | クラウド→エッジ/POS | ✓ | × | 起動時・15分 | 自己更新対応 |
| modules | クラウド→エッジ/POS | ✓ | × | 起動時・15分 | pip, apt等で自動インストール |
| configs | クラウド→エッジ/POS | ✓ | × | 起動時・15分 | 環境設定ファイル |
| images | クラウド→エッジ/POS | ✓ | × | 起動時・15分 | UIアセット |

**注記:**
- ✓: サポート、×: 非サポート
- チェック間隔は環境変数で調整可能（デフォルト: 15分）

#### 2.2.2 コンテナイメージ（クラウド → エッジ/POS）

**対象イメージ:**
- account, terminal, master-data, cart, report, journal, stock, sync

**バージョン管理方法:**
- **バージョンタグ規則**: `<service-name>:v<major>.<minor>.<patch>`
- **初期同期**: 一括同期（全イメージをpull）
- **定期同期**: 差分同期（変更されたイメージのみpull）
- **手動同期**: 管理画面から任意のタイミングで実行可能

**一括同期タイミング:**
- システム初期セットアップ時
- 管理者による手動実行時

**差分同期タイミング:**
- 起動時チェック（エッジ端末、POS端末）
- 15分ごとの定期チェック（エッジ端末、POS端末）

**データソースルーティング:**

Manifestにはイメージの取得元（data_source）が記載され、デバイスはこれに従ってイメージを取得：

- **Edge端末**: `data_source = "cloud"`
  - ACRから直接取得

- **POS端末**: `data_source = "edge"` または `"cloud"`
  - `data_source = "edge"`: 同一店舗内のEdge Registryから取得（高速）
  - `data_source = "cloud"`: ACRから直接取得
  - Edge Registryが利用不可の場合は自動的にACRへフォールバック

**取得フロー（POS端末の場合）:**
1. Manifestの`data_source`を確認
2. `data_source = "edge"`: `primary_registry`からイメージ取得を試行
3. 失敗時: `fallback_registry`（ACR）から取得
4. チェックサム検証後、適用

#### 2.2.3 スクリプトファイル（クラウド → エッジ/POS）

**対象ファイル:**
- edge-startup.sh
- pos-startup.sh

**バージョン管理方法:**
- **差分同期**: チェックサムベースの差分検出
- **自己更新**: スクリプト自身を安全に更新
- **復旧時同期**: ネットワーク復旧時に未適用分を適用

**同期タイミング:**
- 起動時チェック（Phase 1で最優先実行）
- 15分ごとの定期チェック（エッジ端末、POS端末）
- ネットワーク障害時はローカルキャッシュを使用
- ネットワーク復旧時に未適用分を自動適用

**データソースルーティング:**

Manifestには各スクリプトファイルの取得元（data_source）が記載され、デバイスはこれに従ってファイルを取得：

- **Edge端末**: `data_source = "cloud"`
  - Cloud Sync Service API経由で取得

- **POS端末**: `data_source = "edge"` または `"cloud"`
  - `data_source = "edge"`: 同一店舗内のEdge端末のSync Service API経由で取得（高速・ローカルネットワーク）
  - `data_source = "cloud"`: Cloud Sync Service API経由で取得
  - Edge Sync Service APIが利用不可の場合は自動的にCloud Sync Service APIへフォールバック

**重要**: 全てのファイル取得はAPI経由で行われます。ファイルシステムやストレージへの直接アクセスは行いません。

**取得フロー（POS端末の場合）:**
1. Manifestの`data_source`を確認
2. `data_source = "edge"`: `primary_url`（Edge Sync Service API）へリクエスト送信
3. 失敗時: `fallback_url`（Cloud Sync Service API）へリクエスト送信
4. チェックサム検証後、適用

#### 2.2.4 その他ファイル（クラウド → エッジ/POS）

**対象ファイル:**
- モジュール（.whl, .deb, .rpm, .tar.gz）
- 設定ファイル（.conf, .yaml, .json）
- 画像（.png, .jpg, .svg）
- ドキュメント（.pdf, .md）

**同期方法:**
- **差分同期**: チェックサムベースの差分検出
- **復旧時同期**: ネットワーク復旧時に未適用分を適用

**同期タイミング:**
- 起動時チェック
- 15分ごとの定期チェック（エッジ端末、POS端末）

**データソースルーティング:**

スクリプトファイルと同様に、Manifestには各ファイルの取得元が記載：

- **Edge端末**: `data_source = "cloud"`
  - Cloud Sync Service API経由で取得

- **POS端末**: `data_source = "edge"` または `"cloud"`
  - `data_source = "edge"`: Edge Sync Service API経由で取得
  - `data_source = "cloud"`: Cloud Sync Service API経由で取得
  - フォールバック機構により高い可用性を実現

**重要**: 全てのファイル取得はAPI経由で行われます。ファイルシステムやストレージへの直接アクセスは行いません。

**Manifest例（POS端末向け）:**
```json
{
  "artifacts": [
    {
      "type": "script",
      "name": "pos-startup.sh",
      "version": "v1.2.3",
      "data_source": "edge",
      "primary_url": "http://edge-tokyo-001:8007/api/v1/artifact-management/download",
      "fallback_url": "https://sync.kugelpos.cloud/api/v1/artifact-management/download",
      "checksum": "sha256:abc123...",
      "destination": "/opt/kugelpos/pos-startup.sh"
    },
    {
      "type": "config",
      "name": "docker-compose.override.yml",
      "version": "v1.2.3",
      "data_source": "edge",
      "primary_url": "http://edge-tokyo-001:8007/api/v1/artifact-management/download",
      "fallback_url": "https://sync.kugelpos.cloud/api/v1/artifact-management/download",
      "checksum": "sha256:def456...",
      "destination": "/opt/kugelpos/docker-compose.override.yml"
    },
    {
      "type": "module",
      "name": "kugelpos_common-1.2.3-py3-none-any.whl",
      "version": "v1.2.3",
      "data_source": "edge",
      "primary_url": "http://edge-tokyo-001:8007/api/v1/artifact-management/download",
      "fallback_url": "https://sync.kugelpos.cloud/api/v1/artifact-management/download",
      "checksum": "sha256:ghi789...",
      "install_command": "pip install {file}"
    }
  ]
}
```

**注記**:
- Edge端末にもSync Serviceが稼働しており、ローカルにキャッシュされたファイルをAPI経由で配信します
- POS端末は常にAPI経由でファイルを取得し、ファイルシステムには直接アクセスしません

#### 2.2.5 更新処理のフェーズとタイミング

更新処理は以下の9つのフェーズに分かれており、各フェーズは順次実行される。フェーズ間で失敗が発生した場合、適切なロールバック処理が実行される。

**フェーズ1: バージョンチェック**
- **実行タイミング**: 起動時、または15分ごと（定期実行）
- **処理内容**:
  - Sync Serviceに現在のバージョン情報を送信
  - Manifestを取得して更新が必要か判定
- **完了条件**: 更新要否が確定
- **失敗時の動作**: エラーログを記録し、現在のバージョンで起動継続

**フェーズ2: ダウンロード**
- **実行タイミング**: バージョンチェックで更新が必要と判定された直後
- **処理内容**:
  - すべての更新対象ファイル（イメージ、スクリプト等）をダウンロード
  - コンテナイメージは`docker pull`でローカルに保存（まだ適用しない）
  - スクリプトや設定ファイルは一時ディレクトリに保存
- **完了条件**: すべてのファイルがローカルに保存完了
- **失敗時の動作**: ダウンロード済みファイルを削除し、現在のバージョンで起動継続

**フェーズ3: 検証**
- **実行タイミング**: ダウンロード完了直後
- **処理内容**:
  - ダウンロードした各ファイルのチェックサム（SHA256）を検証
  - Manifestに記載されたチェックサムと一致することを確認
- **完了条件**: すべてのファイルのチェックサムが一致
- **失敗時の動作**: ダウンロード済みファイルを削除し、現在のバージョンで起動継続

**フェーズ4: バックアップ**
- **実行タイミング**: 検証完了直後
- **処理内容**:
  - 現在の`docker-compose.yml`をバックアップ
  - 現在のスクリプトファイルをバックアップ
  - 現在のバージョン情報を保存
- **完了条件**: すべての必要ファイルがバックアップ完了
- **失敗時の動作**: バックアップ失敗をログに記録し、更新を中止

**フェーズ5: 適用準備**
- **実行タイミング**: バックアップ完了直後
- **処理内容**:
  - `docker-compose.yml`のイメージタグを新バージョンに更新
  - スクリプトファイルを新バージョンで置き換え
  - 設定ファイルを更新
  - **この時点ではまだサービスは停止していない**
- **完了条件**: すべての設定ファイルが更新完了
- **失敗時の動作**: バックアップから設定ファイルを復元

**フェーズ6: サービス停止**
- **実行タイミング**: 適用準備完了直後
- **処理内容**:
  - `docker-compose down`で既存サービスを停止
  - グレースフルシャットダウン（最大30秒待機）
- **完了条件**: すべてのコンテナが停止
- **失敗時の動作**: 強制停止を実行し、次のフェーズへ進む

**フェーズ7: サービス起動**
- **実行タイミング**: サービス停止完了直後
- **処理内容**:
  - 更新された`docker-compose.yml`で`docker-compose up -d`を実行
  - 新バージョンのコンテナイメージでサービスを起動
  - 起動完了を待機（最大120秒）
- **完了条件**: すべてのコンテナが起動（running状態）
- **失敗時の動作**: フェーズ10のロールバック処理へ移行

**フェーズ8: ヘルスチェック**
- **実行タイミング**: サービス起動完了後、30秒待機してから実行
- **処理内容**:
  - 各サービスの`/health`エンドポイントに対してHTTP GETリクエスト
  - すべてのサービスが200 OKを返すことを確認
  - 最大3回、10秒間隔でリトライ
- **完了条件**: すべてのサービスがヘルスチェックに合格
- **失敗時の動作**: フェーズ10のロールバック処理へ移行

**フェーズ9: 完了通知**
- **実行タイミング**: ヘルスチェック成功直後
- **処理内容**:
  - Sync Serviceに更新完了を通知（`POST /artifact-management/complete`）
  - ローカルのバージョン情報ファイルを更新
  - 古いバックアップファイルを削除
- **完了条件**: 通知が成功（またはオフライン時はキューに保存）
- **失敗時の動作**: エラーログを記録するが、更新は成功とみなす

**フェーズ10: ロールバック（エラー時のみ）**
- **実行タイミング**: フェーズ7またはフェーズ8で失敗した場合
- **処理内容**:
  1. 現在のサービスを停止（`docker-compose down`）
  2. バックアップした`docker-compose.yml`を復元
  3. バックアップしたスクリプトを復元
  4. 旧バージョンでサービスを起動（`docker-compose up -d`）
  5. ヘルスチェックを実行
  6. Sync Serviceに失敗を通知
- **完了条件**: 旧バージョンでサービスが正常起動
- **失敗時の動作**: 管理者に緊急アラートを送信

**重要な時間差:**
- **ダウンロードと適用の時間差**: フェーズ2（ダウンロード）完了からフェーズ6（サービス停止）まで、サービスは稼働継続
- **サービス停止時間**: フェーズ6開始からフェーズ7完了まで（通常1-3分）
- **検証期間**: フェーズ8のヘルスチェックで新バージョンの動作を確認（30-60秒）

#### 2.2.6 2段階更新モード

更新処理を「ダウンロード」と「適用」の2つのステップに分離して実行できる機能を提供します。これにより、営業時間中にファイルをダウンロードしておき、メンテナンス時間帯に適用を実行する運用が可能になります。

**実行モード:**

| モード | コマンドライン | 実行フェーズ | サービス停止 | 用途 |
|--------|--------------|-------------|-------------|------|
| 通常モード | オプションなし | Phase 1-9 | あり | 従来通りの一括更新 |
| ダウンロードのみ | `--download-only` | Phase 1-3 | なし | 営業時間中の事前ダウンロード |
| 適用のみ | `--apply-only` | Phase 3-9 | あり | メンテナンス時間帯の適用 |

**ダウンロードのみモード (`--download-only`):**
- **実行フェーズ**: Phase 1（バージョンチェック）→ Phase 2（ダウンロード）→ Phase 3（検証）
- **処理内容**:
  - Manifestを取得し、更新ファイルをすべてダウンロード
  - チェックサムを検証して完全性を確認
  - ダウンロード済みファイルを`/opt/kugelpos/pending-updates/{version}/`に保存
  - Manifestを`/opt/kugelpos/pending-updates/{version}/manifest.json`に保存
  - ステータスファイルに`download_completed`を記録
- **完了条件**: すべてのファイルがダウンロード・検証完了
- **サービスへの影響**: サービスは継続稼働（停止なし）
- **失敗時の動作**: ダウンロード済みファイルを削除し、エラーを記録

**適用のみモード (`--apply-only`):**
- **実行フェーズ**: Phase 3（検証）→ Phase 4（バックアップ）→ Phase 5（適用準備）→ Phase 6-9（サービス停止・起動・完了）
- **処理内容**:
  - `/opt/kugelpos/pending-updates/`から最新のダウンロード済みファイルを読み込み
  - チェックサムを再検証（ダウンロード後の改ざん防止）
  - 現在の設定をバックアップ
  - サービスを停止して新バージョンを適用
  - ヘルスチェック後、完了通知を送信
- **前提条件**: ダウンロードのみモードが正常完了していること
- **サービスへの影響**: Phase 6-8でサービス停止（通常1-3分）
- **失敗時の動作**: ロールバック処理を実行

**ダウンロード済みファイルの管理:**

ダウンロード済みファイルは以下の構造で管理されます：

```
/opt/kugelpos/pending-updates/
  └── v1.2.3/                          # バージョンディレクトリ
      ├── manifest.json                 # Manifest情報
      ├── status.json                   # ダウンロード状態
      ├── images/                       # コンテナイメージ情報
      │   └── image-list.txt           # pullしたイメージのリスト
      └── artifacts/                    # ファイル群
          ├── pos-startup.sh
          ├── docker-compose.override.yml
          └── kugelpos_common-1.2.3.whl
```

**status.json の例:**
```json
{
  "version": "v1.2.3",
  "download_status": "completed",
  "download_started_at": "2025-01-17T14:30:00Z",
  "download_completed_at": "2025-01-17T14:35:00Z",
  "verification_status": "passed",
  "ready_to_apply": true,
  "artifacts_count": 15,
  "total_size_bytes": 524288000
}
```

**適用タイミングの制御:**

1. **手動適用**: 管理者が任意のタイミングで`--apply-only`を実行
2. **スケジュール適用**: cronやsystemd timerで定時実行
3. **条件付き適用**: メンテナンスウィンドウ内でのみ実行

**運用例:**

```bash
# 営業時間中（14:00）にダウンロードのみ実行
./pos-startup.sh --download-only

# メンテナンス時間帯（02:00）に適用のみ実行
./pos-startup.sh --apply-only
```

**注意事項:**
- ダウンロード済みファイルの有効期限: 7日間（環境変数で調整可能）
- 複数バージョンのダウンロードが存在する場合は最新バージョンを適用
- 適用前に再度チェックサム検証を実施（ダウンロード後の改ざん防止）
- ダウンロードのみモードでは15分間隔のチェックは実行しない（on-demand実行のみ）

### 2.3 更新状態管理

**管理主体**: クラウド側Sync Serviceが全デバイスのバージョン状態を一元管理

**管理対象:**
- `device_type`: デバイスタイプ（edge/pos）
- `device_id`: デバイス識別子
- `current_version`: 現在のバージョン
- `target_version`: 目標バージョン
- `update_status`: 更新状態（idle/checking/downloading/download_completed/applying/completed/failed）
- `update_mode`: 更新モード（normal/download_only/apply_only）
- `pending_version`: ダウンロード済み未適用バージョン
- `last_check_timestamp`: 最終チェック時刻
- `last_update_timestamp`: 最終更新時刻
- `retry_count`: リトライ回数
- `error_message`: エラーメッセージ

#### 2.3.1 更新ステータス

クラウド側Sync Serviceが、各デバイス・配布物種別ごとに以下を管理：
- `device_id`: デバイス識別子
- `artifact_type`: 配布物種別（container_image/script/module/config/image）
- `artifact_name`: 配布物名称
- `current_version`: 現在バージョン
- `target_version`: 目標バージョン
- `last_check_timestamp`: 最終チェック時刻
- `update_status`: 更新状態
- `retry_count`: リトライ回数
- `error_message`: エラーメッセージ

#### 2.3.2 更新履歴

クラウド側Sync Serviceが更新実行履歴を記録：
- デバイスID
- 配布物種別・名称
- 更新タイプ（upgrade/downgrade/rollback）
- 更新開始・終了時刻
- 更新データ件数・サイズ
- 成功/失敗状態
- エラー詳細
- リトライ回数
- 処理時間（ミリ秒）

### 2.4 競合解決

#### 2.4.1 競合検出

- Manifestで指定されたバージョンと現在のバージョンを比較
- チェックサム（SHA256）による変更追跡

#### 2.4.2 解決戦略

**Server Wins (サーバー優先):**
- 唯一の解決方法としてシンプルに実装
- Manifestで指定されたバージョンを常に採用
- 適用対象：全配布物種別
- 更新失敗時は自動ロールバック

### 2.5 エラーハンドリング

#### 2.5.1 リトライ機構

- 指数バックオフによるリトライ
- 最大リトライ回数: 3回（設定可能）
- リトライ間隔: 1秒 → 2秒 → 4秒

#### 2.5.2 自動ロールバック

- ヘルスチェック失敗時の自動ロールバック
- チェックサム検証失敗時の自動ロールバック
- 前バージョンへの即座の復帰

#### 2.5.3 フォールバック

- ネットワーク障害時はローカルキャッシュを使用
- キャッシュ有効期限: 7日間
- 重要な更新は必ず永続化

## 3. 非機能要件

### 3.1 パフォーマンス要件

- **更新時間**: エッジ端末10分以内、POS端末5分以内
- **同時接続**: 1,000台の同時アクセスに対応
- **データ圧縮率**: 50%以上（gzip使用）
- **差分更新効率**: 90%以上の帯域削減（変更がない場合）

### 3.2 可用性要件

- **稼働率**: クラウドサービス99.9%以上、ACR 99.95%以上
- **自動復旧**: ネットワーク復旧後30秒以内に更新再開
- **データ保証**: At-least-once delivery

### 3.3 セキュリティ要件

#### 3.3.1 通信セキュリティ
- **通信暗号化**: TLS 1.3による暗号化
- **チェックサム検証**: SHA256によるファイル改ざん検出

#### 3.3.2 エッジデバイス認証（Edge/POS共通）

**認証フロー:**
1. エッジデバイス（Edge/POS）が`edge_id`を使用して認証リクエスト送信
2. クラウド側Sync Serviceが認証し、JWTトークンを発行
3. 以降のAPIアクセスはJWTトークンをAuthorizationヘッダーに付与

**デバイス識別:**
- Edge端末: `edge_id` (例: "edge-tokyo-001")
- POS端末: `edge_id` (例: "pos-tokyo-001-terminal-01")
- どちらもバージョン管理上は同じエッジデバイスとして扱う

**注記:**
- POS端末は業務処理においては`terminal_id`と`api_key`でTerminal Service経由で認証
- バージョン管理においては`edge_id`でSync Serviceに直接認証
- これにより認証フローがシンプルになり、Terminal Serviceへの依存を削減

### 3.4 運用要件

- **監視**: メトリクス、ログ、ヘルスチェック
- **アラート**: 更新失敗、エラー率上昇時に通知
- **バックアップ**: バージョン設定の定期バックアップ

#### 3.4.1 2段階更新の運用ガイドライン

**推奨運用パターン:**

1. **POS端末での運用:**
   - **ダウンロード**: 営業時間中（14:00-16:00）に実行
   - **適用**: 営業終了後のメンテナンス時間帯（02:00-05:00）に実行
   - **確認**: 営業開始前にヘルスチェックを実施

2. **Edge端末での運用:**
   - **ダウンロード**: 業務時間中（10:00-18:00）に実行
   - **適用**: 深夜のメンテナンスウィンドウ（01:00-04:00）に実行
   - **確認**: 適用後に店舗内POS端末への影響を確認

**スケジュール例（cron）:**

```bash
# POS端末: 14:00にダウンロード
0 14 * * * /opt/kugelpos/pos-startup.sh --download-only >> /var/log/kugelpos/update-download.log 2>&1

# POS端末: 02:00に適用
0 2 * * * /opt/kugelpos/pos-startup.sh --apply-only >> /var/log/kugelpos/update-apply.log 2>&1
```

**監視項目:**

- ダウンロード完了状態の監視（`/opt/kugelpos/pending-updates/*/status.json`）
- ダウンロード済みファイルの有効期限チェック
- 適用待ちバージョンの存在確認
- 適用失敗時のアラート通知

**注意事項:**

- ダウンロードのみモードは定期実行（15分間隔チェック）とは独立して動作
- 適用のみモードはダウンロード済みファイルが存在しない場合はエラーで終了
- 複数バージョンがダウンロード済みの場合、最新バージョンを優先して適用

## 4. インターフェース仕様

### 4.1 エッジデバイス認証API（Edge/POS共通）

```
POST /api/v1/version-management/auth
Headers:
  Content-Type: application/json

Request:
{
  "edge_id": "edge-tokyo-001"  // Edge端末の場合
  // または
  "edge_id": "pos-tokyo-001-terminal-01"  // POS端末の場合
}

Response: 200 OK
{
  "success": true,
  "data": {
    "access_token": "eyJhbGciOiJIUzI1NiIs...",
    "token_type": "bearer",
    "expires_in": 3600,
    "device_type": "edge",  // or "pos"
    "device_id": "edge-tokyo-001"
  }
}

Error Response: 401 Unauthorized
{
  "success": false,
  "error": {
    "code": "AUTH_002",
    "message": "Invalid edge_id"
  }
}
```

### 4.2 バージョン管理API

#### 4.2.1 バージョン確認（デバイスからの定期リクエスト）

```
POST /api/v1/artifact-management/check
Headers:
  Authorization: Bearer <JWT_TOKEN>
  X-Device-ID: <device_id>
  X-Device-Type: edge|pos
  Content-Type: application/json

Request:
{
  "device_type": "edge",
  "device_id": "edge-tokyo-001",
  "current_version": "v1.2.2",
  "current_checksums": {
    "edge-startup.sh": "sha256:abc123...",
    "cart": "sha256:def456..."
  }
}

Response: 200 OK
{
  "success": true,
  "data": {
    "updates_required": true,
    "target_version": "v1.2.3",
    "artifacts": [
      {
        "type": "script",
        "name": "edge-startup.sh",
        "version": "v1.2.3",
        "data_source": "cloud",  // Edge端末の場合は"cloud"、POS端末の場合は"edge"
        "primary_url": "https://sync.kugelpos.cloud/api/v1/artifact-management/download",  // Edge: Sync経由、POS: Edge端末
        "fallback_url": null,  // Edge端末の場合はnull、POS端末の場合はSync経由のURL
        "checksum": "sha256:...",
        "size": 15234,
        "destination": "/opt/kugelpos/scripts/edge-startup.sh",
        "permissions": "0755",
        "priority": 1,
        "required": true
      }
    ],
    "container_images": [
      {
        "service": "cart",
        "version": "v1.2.3",
        "data_source": "edge",  // "cloud" or "edge"
        "primary_registry": "edge-tokyo-001:5000",  // Edge Registry in same store
        "primary_image": "cart:v1.2.3",
        "fallback_registry": "masakugel.azurecr.io",  // Cloud ACR as fallback
        "fallback_image": "production/services/cart:v1.2.3",
        "checksum": "sha256:..."
      }
    ],
    "update_policy": {
      "window": "02:00-05:00",
      "auto_update": true,
      "force_update": false
    }
  }
}
```

#### 4.2.2 手動更新実行

```
POST /api/v1/artifact-management/execute
Headers:
  Authorization: Bearer <JWT_TOKEN>
  Content-Type: application/json

Request:
{
  "device_id": "edge-tokyo-001",
  "target_version": "v1.2.3",
  "force_update": false
}

Response: 202 Accepted
{
  "success": true,
  "data": {
    "update_id": "UPDATE_A1234_EDGE001_01JK3X9Y5Z8QWERTYU",
    "status": "queued",
    "message": "Update request has been queued for processing"
  }
}
```

#### 4.2.3 更新履歴取得

```
GET /api/v1/artifact-management/history?device_id=edge-tokyo-001&from=2025-01-01&to=2025-01-15
Headers:
  Authorization: Bearer <JWT_TOKEN>

Response: 200 OK
{
  "success": true,
  "data": {
    "histories": [
      {
        "update_id": "UPDATE_A1234_EDGE001_01JK3X9Y5Z8QWERTYU",
        "device_id": "edge-tokyo-001",
        "from_version": "v1.2.2",
        "to_version": "v1.2.3",
        "update_type": "upgrade",
        "start_time": "2025-01-15T10:30:00Z",
        "end_time": "2025-01-15T10:35:00Z",
        "status": "success"
      }
    ],
    "total": 1
  }
}
```

### 4.3 Artifact配布API

#### 4.3.1 ファイルダウンロード（デバイスからクラウドへ）

```
POST /api/v1/artifact-management/download
Headers:
  Authorization: Bearer <JWT_TOKEN>
  X-Device-ID: <device_id>
  X-Device-Type: edge|pos
  Content-Type: application/json

Request:
{
  "artifact_type": "script",
  "artifact_name": "edge-startup.sh",
  "version": "v1.2.3",
  "checksum": "sha256:abc123..."
}

Response: 200 OK
Headers:
  Content-Type: application/octet-stream
  Content-Disposition: attachment; filename="edge-startup.sh"
  X-Checksum: sha256:abc123...
  X-File-Size: 15234

Body: <file binary data>
```

#### 4.3.2 ダウンロード完了通知（2段階更新用）

```
POST /api/v1/artifact-management/download-complete
Headers:
  Authorization: Bearer <JWT_TOKEN>
  X-Device-ID: <device_id>
  X-Device-Type: edge|pos
  Content-Type: application/json

Request:
{
  "device_type": "pos",
  "device_id": "pos-tokyo-001-terminal-01",
  "target_version": "v1.2.3",
  "download_status": "completed",
  "download_started_at": "2025-01-17T14:30:00Z",
  "download_completed_at": "2025-01-17T14:35:00Z",
  "artifacts_count": 15,
  "total_size_bytes": 524288000,
  "pending_updates_path": "/opt/kugelpos/pending-updates/v1.2.3",
  "timestamp": "2025-01-17T14:35:00Z"
}

Response: 200 OK
{
  "success": true,
  "data": {
    "status": "download_acknowledged",
    "message": "Download completed. Ready for apply phase.",
    "pending_version": "v1.2.3"
  }
}
```

#### 4.3.3 更新完了通知

```
POST /api/v1/artifact-management/complete
Headers:
  Authorization: Bearer <JWT_TOKEN>
  X-Device-ID: <device_id>
  X-Device-Type: edge|pos
  Content-Type: application/json

Request:
{
  "device_type": "edge",
  "device_id": "edge-tokyo-001",
  "target_version": "v1.2.3",
  "update_mode": "normal",  // "normal", "download_only", "apply_only"
  "update_results": [
    {
      "name": "edge-startup.sh",
      "type": "script",
      "version": "v1.2.3",
      "status": "success",
      "error": null
    },
    {
      "service": "cart",
      "type": "container_image",
      "version": "v1.2.3",
      "status": "success",
      "error": null
    }
  ],
  "timestamp": "2025-01-15T10:35:00Z"
}

Response: 200 OK
{
  "success": true,
  "data": {
    "status": "acknowledged",
    "next_check": "2025-01-15T16:35:00Z"
  }
}
```

#### 4.3.4 ロールバック指示

```
POST /api/v1/artifact-management/rollback
Headers:
  Authorization: Bearer <ADMIN_TOKEN>
  Content-Type: application/json

Request:
{
  "device_id": "edge-tokyo-001",
  "rollback_to_version": "v1.2.2",
  "reason": "Performance degradation detected"
}

Response: 200 OK
{
  "success": true,
  "data": {
    "status": "rollback_initiated",
    "target_version": "v1.2.2",
    "applied_at": null
  }
}
```

## 5. 配布物詳細仕様

### 5.1 ファイルカテゴリ

| カテゴリ | type値 | 拡張子例 | 用途 |
|---------|--------|---------|------|
| スクリプト | script | .sh, .bash | 起動・更新処理 |
| モジュール | module | .whl, .deb, .rpm, .tar.gz | プログラム機能追加 |
| 画像 | image | .png, .jpg, .svg | UI表示 |
| 設定 | config | .conf, .yaml, .json | サービス設定 |
| ドキュメント | doc | .pdf, .md | マニュアル |

### 5.2 各カテゴリの処理要件

#### スクリプトファイル
- バックアップを自動作成（前バージョンのみ保持）
- 実行権限を自動設定（755）
- ロールバック時は前バージョンに復元

#### モジュール
- Python: pip install コマンドで自動インストール
- システムパッケージ: apt/yum で自動インストール
- ロールバック: パッケージマネージャーの機能を利用

#### 画像ファイル
- 指定パスにコピー
- 権限は644
- ロールバック時は前バージョンに復元

#### 設定ファイル
- 展開処理（tar.gz等）をサポート
- 環境変数の置換（オプション）
- ロールバック時は前バージョンに復元

#### ドキュメント
- 指定パスにコピー
- 圧縮形式もサポート
- ロールバック時は前バージョンに復元

## 6. データベース構成

### 6.1 テナント別データベース

- バージョン管理サービスも他のサービスと同様に、テナント毎に独立したデータベースを使用
- データベース名: `sync_{tenant_id}` (例: sync_A1234)
- 各テナントのバージョン管理状態、履歴、デバイス情報は完全に分離して管理

**ドキュメントバージョン**: 1.0.0
**作成日**: 2025-01-17
**最終更新日**: 2025-01-17
