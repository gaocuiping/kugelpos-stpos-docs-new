import os

base = '/home/gaocuiping/myself/kugelpos-stpos-docs-new/docs'

ja_account = """---
title: "Account サービス テストケース"
parent: テスト
grand_parent: 日本語
nav_order: 11
layout: default
---

# Account サービス テスト設計書

本ドキュメントは、Account サービスのテスト網羅性と品質担保のための詳細なテスト設計書です。

## 1. サービスの概要とテスト戦略

**Account サービス**は、システムの入り口となる「認証・認可」と「テナント設定」を管理する最重要コンポーネントです。
セキュリティに関わるため、正常系だけでなく**異常系（パスワード間違い、トークン期限切れ、不正アクセス）**のテストカバレッジを 100% に近づける必要があります。

### 1.1 前提条件・テストデータ
- **依存サービス**: MongoDB, Dapr StateStore (Redis)
- **テスト用テナント**: `tenant-test-01`
- **モック設定**: DBアクセスは `AsyncMock` 等でモック化し、外部依存を排除した高速なテストを実現すること。

---

## 2. ユニットテスト (API・ロジック単位)

機能ドメインやAPIエンドポイントごとの詳細なテストケースです。

### 2.1 認証プロセス (Authentication)

| ID | ターゲットAPI | テストシナリオ (Before/When/Then) | 期待される結果 | 状態 |
|----|-------------|--------------------------------|--------------|------|
| **AC-U-001** | `POST /login` | 登録済みの正しいID・パスワードでログインする | `200 OK`、有効な JWT トークン (access/refresh) が返却されること | ✅ 実装済 |
| **AC-U-002** | `POST /login` | 登録されていないユーザーIDでログインを試みる | `401 Unauthorized` が返却されること | ❌ 推奨 |
| **AC-U-003** | `POST /login` | 正しいIDだが、誤ったパスワードでログインを試みる | `401 Unauthorized` が返却されること | ❌ 推奨 |
| **AC-U-004** | `POST /login` | パスワードが空、またはリクエスト形式が不正な場合 | `422 Validation Error` が返却されること | ❌ 推奨 |
| **AC-U-005** | `POST /refresh` | 有効なリフレッシュトークンで新しいトークンを要求する | `200 OK`、新しいアクセストークンが返却されること | ❌ 推奨 |
| **AC-U-006** | `POST /refresh` | 期限切れ、または無効なリフレッシュトークンを使用する | `401 Unauthorized` が返却されること | ❌ 推奨 |

### 2.2 マルチテナント検証 (Tenant Context)

| ID | ターゲットAPI | テストシナリオ (Before/When/Then) | 期待される結果 | 状態 |
|----|-------------|--------------------------------|--------------|------|
| **AC-U-010** | Any (Auth API) | `Tenant-ID` ヘッダーを付けてリクエストする | リクエストが正常に処理され、指定テナント用のDBコンテキストにアクセスすること | ❌ 推奨 |
| **AC-U-011** | Any (Auth API) | `Tenant-ID` ヘッダー**なし**でリクエストする | `400 Bad Request` (テナントID必須) が返却されること | ❌ 推奨 |

### 2.3 ユーザー管理 (User Management)

| ID | ターゲットAPI | テストシナリオ (Before/When/Then) | 期待される結果 | 状態 |
|----|-------------|--------------------------------|--------------|------|
| **AC-U-020** | `GET /me` | 有効な Bearer トークンを指定してプロフィール取得 | `200 OK`、パスワードを含まないユーザー情報が返却されること | ❌ 推奨 |
| **AC-U-021** | `POST /users` | 新規管理ユーザー（ユニークなID）の作成 | `201 Created`、DBにパスワードがハッシュ化されて保存されること | ❌ 推奨 |
| **AC-U-022** | `POST /users` | 既に存在するユーザーIDで作成を試みる | `409 Conflict` が返却されること | ❌ 推奨 |

---

## 3. インテグレーションテスト (統合検証)

| ID | コンポーネント | テストシナリオ | 確認ポイント | 状態 |
|----|--------------|--------------|------------|------|
| **AC-I-001** | MongoDB | JWT ペイロードのユーザーIDから MongoDB への実クエリ（Read/Write） | テストDBに実データが保存され、正しく取得できること | 🟠 一部 |
| **AC-I-002** | JWT Engine | サードパーティライブラリとの連携（PyJWT） | トークンの署名、検証、期限チェックが設計通りに動作すること | ❌ 推奨 |

---

## 4. エンドツーエンド (E2E) シナリオテスト

| ID | シナリオフロー | 期待される結果とアサーション | 状態 |
|----|--------------|--------------------------|------|
| **AC-S-001** | **正常ログインフロー** <br>1. `/login` で認証<br>2. 取得したトークンで `/me` アクセス<br>3. トークンの有効性を確認 | 全て200系で成功し、トークンデコード結果とDBデータが一致すること | ❌ 推奨 |
| **AC-S-002** | **タイムアウトフロー** <br>1. ログイン後、Token期限の15分を待機（※時刻操作モック）<br>2. `/me` アクセス (結果: 401)<br>3. `/refresh` 実行 | アクセストークンの期限切れが検知され、リフレッシュ処理が機能すること | ❌ 推奨 |

---

## 5. エラーハンドリング・セキュリティマトリクス

| エラー種別 | トリガー条件 | 期待される HTTP ステータス | 期待される内部処理 |
|-----------|------------|------------------------|-------------------|
| パスワード漏洩耐性 | プレーンテキストでの保存試行 | `500` / `400` | BCrypt 非適用での保存はアプリケーション側でブロックされること |
| XSS / Injection | IDや名前フィールドに `<script>` 等を含む | `422` / サニタイズ | Pydantic によるサニタイズ、または拒否 |
| 不正署名トークン | `alg` ヘッダーを変更、または署名偽装 | `401 Unauthorized` | Signature Verification Failed がログに記録される |
"""

ja_cart = """---
title: "Cart サービス テストケース"
parent: テスト
grand_parent: 日本語
nav_order: 14
layout: default
---

# Cart サービス テスト設計書

本ドキュメントは、POSシステムの中核をなす Cart サービスの詳細なテストケースとシナリオです。

## 1. サービスの概要とテスト戦略

**Cart サービス**は、「スキャン → 小計 → 割引 → 決済 → 確定」という最も複雑な業務ロジックを持ちます。
税計算、小計ロジック、二重送信（二重Void/Refund）防止などがクリティカルとなるため、シナリオテストを通じた**整合性の検証**が最重要です。

### 1.1 前提条件・テストデータ
- **依存サービス**: Master Data (gRPC), Terminal (REST), Stock & Journal (Dapr Pub/Sub)
- **テスト用マスターデータ**: 内税商品、外税商品、単一税率、複数税率の組み合わせを含むこと。

---

## 2. ユニットテスト (API・ロジック単位)

### 2.1 商品登録・カート状態管理 (Item Entry & State)

| ID | ターゲットAPI | テストシナリオ (Before/When/Then) | 期待される結果 | 状態 |
|----|-------------|--------------------------------|--------------|------|
| **CT-U-001** | `POST /entry` | JANコードをスキャンして商品を1点登録する | Master Data から情報取得し、カートに商品が追加されること | ✅ 実装済 |
| **CT-U-002** | `resume_item_entry` | 強制終了後の再開（レジューム）処理 | dapr statestore から前回のカート状態が復元されること | ✅ 実装済 |
| **CT-U-003** | `DELETE /entry/{id}`| 登録した商品の一部を取消（行キャンセル）する | 指定された行が削除され、小計が再計算されること | ❌ 推奨 |

### 2.2 金額・税・割引計算 (Calculation Engine)

| ID | ターゲットモジュール | テストシナリオ (Before/When/Then) | 期待される結果 | 状態 |
|----|------------------|--------------------------------|--------------|------|
| **CT-U-010** | `calc_subtotal` | 複数商品の合計金額計算（税抜・税込混在） | 精度落ちなく1円単位まで正確に小計が計算されること | ✅ 実装済 |
| **CT-U-011** | `tax_engine` | 単一商品に複数の税率（例：消費税10% + 軽減税率8%） | 各税金が正しく按分・計算されること | ❌ 推奨 |
| **CT-U-012** | `discount_engine`| 単品「100円引き」と全体「10%引き」の複合 | 割引適用順序（単品→全体）に従い、正確な最終金額を算出すること | ❌ 推奨 |
| **CT-U-013** | `calc_subtotal` | 割引適用によって金額がマイナスになる場合 | エラーまたは0円下限として処理されること | ❌ 推奨 |

### 2.3 決済とトランザクション状態 (Payment & Status)

| ID | ターゲットAPI | テストシナリオ (Before/When/Then) | 期待される結果 | 状態 |
|----|-------------|--------------------------------|--------------|------|
| **CT-U-020** | `POST /payment` | 現金での全額支払い完了 | トランザクションステータスが「完了」になること | ✅ 実装済 |
| **CT-U-021** | `POST /payment` | キャッシュレス決済中の通信エラー等 | ロールバックされ、ステータスが未決に維持されること | ✅ 実装済 |
| **CT-U-022** | `Void/Return` | 既に取り消し済みの取引に対する再取消（二重Void） | 二重実行がブロックされ、例外が発生すること | ✅ 実装済 |

---

## 3. インテグレーションテスト (統合検証)

| ID | コンポーネント連携 | テストシナリオ | 確認ポイント | 状態 |
|----|-----------------|--------------|------------|------|
| **CT-I-001** | Cart → Master Data | gRPC を用いた商品情報の直接取得 | キャッシュ・フェイルオーバーが機能し、高速に応答すること | ✅ 実装済 |
| **CT-I-002** | Cart → Dapr (Stock) | 購入完了時の Dapr Pub/Sub 経由の在庫引当通知 | メッセージが正しくブローカーに送信されること | ❌ 推奨 |
| **CT-I-003** | Cart → Dapr (Journal) | 決済完了後のジャーナル生成通知 | 取引詳細データ（JSON）が正しく構築・送信されること | ❌ 推奨 |

---

## 4. エンドツーエンド (E2E) シナリオテスト

| ID | シナリオフロー (item-book scenario) | 期待される結果とアサーション | 状態 |
|----|-----------------------------------|--------------------------|------|
| **CT-S-001** | **標準的買い物フロー** <br>1. 商品 A, B をスキャン<br>2. 商品 A の数量を 3 に変更<br>3. 商品 B に 5% 割引適用<br>4. 現金とクレジットカードで分割払い(Split) | 計算結果が全て正確であり、最終的に Dapr イベントが発行されること | ❌ 推奨 |
| **CT-S-002** | **返品フロー** <br>1. 過去のトランザクションIDを指定して返品依頼<br>2. マイナス金額でのジャーナル登録 | 対象トランザクションが Refunded になり、二重返品が拒否されること | ❌ 推奨 |
"""

with open(os.path.join(base, 'ja', 'testing', 'testcases-account.md'), 'w') as f: f.write(ja_account)
with open(os.path.join(base, 'ja', 'testing', 'testcases-cart.md'), 'w') as f: f.write(ja_cart)

# English versions can just be placeholders that point to the JA ones for now, or translated.
# I will output the script to translate the strings easily.
en_account = ja_account.replace('日本語', 'English').replace('テスト', 'Testing').replace('テスト設計書', 'Test Specification').replace('実装済', 'Implemented').replace('推奨', 'Recommended')
en_cart = ja_cart.replace('日本語', 'English').replace('テスト', 'Testing').replace('テスト設計書', 'Test Specification').replace('実装済', 'Implemented').replace('推奨', 'Recommended')

with open(os.path.join(base, 'en', 'testing', 'testcases-account.md'), 'w') as f: f.write(en_account)
with open(os.path.join(base, 'en', 'testing', 'testcases-cart.md'), 'w') as f: f.write(en_cart)

print("Generated enhanced test case templates for Account and Cart!")
